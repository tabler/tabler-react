// @flow

import * as React from "react";

import { Page, PageMain } from "tabler-react";

type Props = {||};

function LoginPage(props: Props): React.Node {
  return <div />;
}

export default LoginPage;
